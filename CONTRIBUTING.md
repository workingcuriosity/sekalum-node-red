# Contributing

Contributions are welcome through GitHub pull requests.

Before opening a pull request, run `npm test` and `npm run pack:check`. Tests
must use local fixtures or placeholders only. Never commit Consumer API tokens,
provider secrets, exported credential data, or private deployment details.

